spring.application.name=event-management-system

# ---------- Server ----------
server.port=${PORT:8080}

# ---------- MySQL ----------
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ---------- JWT ----------
# Base64-encoded secret (>= 256 bits). Generate your own before deploying.
app.jwt.secret=${JWT_SECRET:c29tZXZlcnlsb25nc2VjcmV0a2V5Zm9yand0c2lnbmluZ3B1cnBvc2VzMTIzNDU2Nzg=}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# ---------- Mail (Gmail SMTP) ----------
# Use a Google "App Password", NOT your normal Gmail password.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${USER_EMAIL:samplework8085@gmail.com}
spring.mail.password=${USER_PASSWORD:afsv rilv pcmp hyzn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ---------- Admin seed account ----------
app.admin.email=${ADMIN_EMAIL:admin@eventms.com}
app.admin.password=${ADMIN_PASSWORD:Admin@123}
app.admin.name=${ADMIN_NAME:System Admin}
